# Import the datetime class from the datetime module.
import datetime as dt

#use the now() attribute on the datetime class to get the present time.
now = dt.datetime.now()

# Print the prestent time.
print("the time right now ", now)